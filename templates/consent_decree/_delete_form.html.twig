<form method="post" action="{{ path('consent_decree_delete', {'id': consent_decree.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consent_decree.id) }}">
    <button class="btn">Delete</button>
</form>
